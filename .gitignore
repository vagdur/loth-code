node_modules
dist

# Staged into the npm tarball by scripts/stage-package-assets.mjs; the real
# copy lives in @vagdur/exsurge.
html/ExsurgeChar.otf

# Incremental Gregorio `.gtex` cache for fixture compiles (reproducible).
tests/fixtures/.compile-cache/

# Python bytecode and caches (scripts/build-sanctoral-calendars.py)
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/

# Artifacts from the TeX compiler:
*.aux
*.log
*.fls
*.fdb_latexmk
*.toc
*.out
